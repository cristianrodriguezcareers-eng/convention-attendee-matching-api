# Convention Attendee Matching API

This is a **mini backend API** built with **Python and Flask** for a project that connects individuals attending conventions related to **toys, cosplay, TCG, and video games**.

The API allows you to:

- Check if the server is running
- Retrieve a list of attendees that match a given user’s convention and interests

---

## Project Structure

